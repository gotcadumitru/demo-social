import React from 'react';
import s from './Settings.module.css'

function Settings(propt) {
    return (
        <div className={s.content}>
            My Settings
        </div>
    );
}
export default Settings;